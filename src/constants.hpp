#pragma once

namespace myHydro
{
    // Math constants
    static const double zero;
    static const double pi4;
    static const double pi4_sq;
    static const double one_third;
    static const double four_thirds;
    static const double pi4_3;

    // Newton gravitation constant
    static const double G;

    // Nuclear density
    static const double rhoNuc;

    // Polytropic (gamma = 4/3) pressure constant
    static const double K4_3;

    // Polytropic (gamma = 2) pressure constant
    static const double K2;

    // Central density
    static const double rhoc;
}